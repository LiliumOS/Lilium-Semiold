
srcdir = @srcdir@
builddir = @builddir@

RUSTC = @RUSTC@
RUSTFLAGS = @RUSTFLAGS@
CC = @CC@
CFLAGS = @CFLAGS@

INSTALL = @INSTALL@

RUST_TARGET_PATH = @RUST_TARGET_PATH@

prefix = @prefix@
includedir = @libdir@

bootdir = @bootdir@
kernel_libdir = @kernel_libdir@
search_paths = @search_paths@
base_dirs = @base_dirs@
kernel_name = @kernel_name@

MKDIR_P = @MKDIR_P@

# all







all: Makefile
	touch stamp
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-panic-halt/all 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-fs-drivers/all 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-loader/all 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-kernel/all 



Makefile: config.status
	./config.status Makefile

config.status: @srcdir@/configure
	./config.status --recheck

clean:
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-panic-halt/clean 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-fs-drivers/clean 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-loader/clean 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-kernel/clean 


install:
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-panic-halt/install 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-fs-drivers/install 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-loader/install 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-kernel/install 


install-strip:
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-panic-halt/install-strip 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-fs-drivers/install-strip 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-loader/install-strip 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  phantom-kernel/install-strip 


# configure and build subdirs



.PHONY: configure-phantom-panic-halt phantom-panic-halt/all phantom-panic-halt/install phantom-panic-halt/install-strip phantom-panic-halt/clean

configure-phantom-panic-halt:
	$(MKDIR_P) phantom-panic-halt
	cd phantom-panic-halt && ../$(srcdir)/phantom-panic-halt/configure  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -prefix=$(prefix) --includedir=$(includedir) --with-kernel-libdir=$(kernel_libdir) --with-bootdir=$(bootdir) --build=@build@ --host=@host@

phantom-panic-halt/: 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  configure-phantom-panic-halt

phantom-panic-halt/all: phantom-panic-halt/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-panic-halt all

phantom-panic-halt/install: phantom-panic-halt/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-panic-halt install

phantom-panic-halt/install-strip: phantom-panic-halt/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-panic-halt install-strip

phantom-panic-halt/clean: phantom-panic-halt/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-panic-halt clean


.PHONY: configure-phantom-fs-drivers phantom-fs-drivers/all phantom-fs-drivers/install phantom-fs-drivers/install-strip phantom-fs-drivers/clean

configure-phantom-fs-drivers:
	$(MKDIR_P) phantom-fs-drivers
	cd phantom-fs-drivers && ../$(srcdir)/phantom-fs-drivers/configure  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -prefix=$(prefix) --includedir=$(includedir) --with-kernel-libdir=$(kernel_libdir) --with-bootdir=$(bootdir) --build=@build@ --host=@host@

phantom-fs-drivers/: 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  configure-phantom-fs-drivers

phantom-fs-drivers/all: phantom-fs-drivers/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-fs-drivers all

phantom-fs-drivers/install: phantom-fs-drivers/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-fs-drivers install

phantom-fs-drivers/install-strip: phantom-fs-drivers/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-fs-drivers install-strip

phantom-fs-drivers/clean: phantom-fs-drivers/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-fs-drivers clean


.PHONY: configure-phantom-loader phantom-loader/all phantom-loader/install phantom-loader/install-strip phantom-loader/clean

configure-phantom-loader:
	$(MKDIR_P) phantom-loader
	cd phantom-loader && ../$(srcdir)/phantom-loader/configure  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -prefix=$(prefix) --includedir=$(includedir) --with-kernel-libdir=$(kernel_libdir) --with-bootdir=$(bootdir) --build=@build@ --host=@host@

phantom-loader/: 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  configure-phantom-loader

phantom-loader/all: phantom-loader/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-loader all

phantom-loader/install: phantom-loader/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-loader install

phantom-loader/install-strip: phantom-loader/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-loader install-strip

phantom-loader/clean: phantom-loader/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-loader clean


.PHONY: configure-phantom-kernel phantom-kernel/all phantom-kernel/install phantom-kernel/install-strip phantom-kernel/clean

configure-phantom-kernel:
	$(MKDIR_P) phantom-kernel
	cd phantom-kernel && ../$(srcdir)/phantom-kernel/configure  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -prefix=$(prefix) --includedir=$(includedir) --with-kernel-libdir=$(kernel_libdir) --with-bootdir=$(bootdir) --build=@build@ --host=@host@

phantom-kernel/: 
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  configure-phantom-kernel

phantom-kernel/all: phantom-kernel/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-kernel all

phantom-kernel/install: phantom-kernel/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-kernel install

phantom-kernel/install-strip: phantom-kernel/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-kernel install-strip

phantom-kernel/clean: phantom-kernel/
	+$(MAKE)  CC="$(CC)" CFLAGS="$(CFLAGS)" RUSTC="$(RUSTC)" RUSTFLAGS="$(RUSTFLAGS)" prefix="$(prefix)" includedir="$(includedir)" kernel_libdir="$(kernel_libdir)" bootdir="$(bootdir)" RUST_TARGET_PATH="$(RUST_TARGET_PATH)"  -C phantom-kernel clean
